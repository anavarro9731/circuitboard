trigger:
  branches:
    include:
    - master
    - releases/*
  paths:
    include:
    - src/.version

pool:
  vmImage: 'windows-latest'

steps:

- powershell: |
   & $Env:BUILD_SOURCESDIRECTORY\src\posh-bootstrap.ps1
   Run -BuildAndTest   
  displayName: 'Build and Test'

- powershell: |
   & $Env:BUILD_SOURCESDIRECTORY\src\posh-bootstrap.ps1
   Run -PackAndPublish -nugetApiKey oy2c44tuzqq5p2gdrm6qaqkxec4vitxebbrgeboadatixy   
  displayName: 'Pack and Publish'
